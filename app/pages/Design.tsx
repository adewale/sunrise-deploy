import { Item, Stat } from './_shared';

export default function Design() {
  const sampleItem = { id: 'design-sample', canonicalSubjectKey: 'design:sample', kind: 'authored_pr_failing', title: 'Authored PR has failing checks', repo: 'owner/repo', url: '#', updatedAt: new Date().toISOString(), reason: 'The item row, chips, title, reason, and action copy reuse the dashboard renderer.', suggestedAction: 'Fix failing checks', evidence: { checks: 'failure' }, source: 'pulls' };
  return <><header className="masthead panel"><div><p className="eyebrow">Design language</p><h1>Interface kit</h1><p className="muted">This page is public and reuses the same components as the product UI, so changes to originals update here too.</p></div><a className="button ghost" href="/">Back home</a></header><section className="metrics"><Metric label="Assigned" value={3} /><Metric label="Mentioned" value={7} /><Metric label="Authored PRs" value={12} /><Metric label="Reviews" value={2} /></section><section className="section panel"><div className="section-head"><div><p className="eyebrow">Type</p><h2>Warm, crisp, operational</h2></div><span className="badge">public</span></div><p className="muted">Fraunces gives Sunrise its personality. IBM Plex Sans keeps dense dashboard text readable. IBM Plex Mono marks system labels, chips, code, and table headers.</p><p className="actions"><a className="button primary" href="#">Primary action</a><button className="button ghost">Secondary action</button><button className="button quiet">Quiet action</button></p></section><section className="section panel"><div className="section-head"><div><p className="eyebrow">Action item</p><h2>Dashboard row</h2></div><span className="badge">example</span></div><div className="item-list"><Item item={sampleItem as any} /></div></section><section className="section panel"><div className="section-head"><div><p className="eyebrow">Setup pattern</p><h2>First-boot cards</h2></div></div><div className="deploy-card"><div><strong>Start with one-click deploy</strong><p>Cards, copy, buttons, and code styling mirror onboarding.</p></div><a className="button primary" href="#">Deploy to Cloudflare</a></div><div className="config-card"><p><span>Homepage URL</span><code>https://sunrise.example.workers.dev</code></p><p><span>Callback URL</span><code>https://sunrise.example.workers.dev/callback</code></p></div></section><section className="section panel"><div className="section-head"><div><p className="eyebrow">Table</p><h2>Runs table</h2></div></div><table><thead><tr><th>Started</th><th>Trigger</th><th>Status</th><th>Candidates</th></tr></thead><tbody><tr><td>2026-05-01</td><td>manual</td><td><span className="badge">succeeded</span></td><td>18</td></tr></tbody></table></section></>;
}

function Metric({ label, value }: { label: string; value: number }) { return <div className="metric panel"><span>{label}</span><strong>{value}</strong></div>; }
